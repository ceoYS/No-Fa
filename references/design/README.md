# NoF Design References

These files are design references only.

Do not copy standalone HTML into the React app.
Do not replace the current app architecture with the mockup HTML.
Do not clone benchmark apps pixel-perfect.

Use these references to extract:
- timer-first home hierarchy
- large abstinence elapsed time
- crisis CTA placement
- relapse/restart confirmation flow
- check-in as secondary daily logging
- pet room as secondary emotional reward layer

Source of truth:
- current React source in src/
- current routes/state/reward guards
- NoF ink-and-ember visual identity
